CREATE TABLE Stargates
(
	Id INTEGER AUTO_INCREMENT,
	Name VARCHAR(128),
	GateData VARBINARY(8000),
	PRIMARY KEY(Id),
	CONSTRAINT UNIQUE_SNAME UNIQUE (Name)
);

CREATE TABLE VersionInfo
(
	Version INTEGER
);

INSERT INTO VersionInfo VALUES (1);